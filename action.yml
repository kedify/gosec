name: "Gosec Security Checker"
description: "Runs the gosec security checker"
author: "@ccojocar"

inputs:
  args:
    description: "Arguments for gosec"
    required: true
    default: "-h"

runs:
  using: "docker"
  image: "docker://ghcr.io/kedify/securego/gosec:v2.22.10"
  args:
    - ${{ inputs.args }}

branding:
  icon: "shield"
  color: "blue"
